import { vehicleModel } from './vehicle.model';
import { dispositiveModel } from './dispositive.model';
export class listVehicleDispositiveModel {
  public 	vehicle: vehicleModel;
  public	dispositive: Array<dispositiveModel>;
  }
